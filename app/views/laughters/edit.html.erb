    <% if @laughter.errors.any? %>
      <div class="ui negative message">
      <i class="close icon"></i>
        <% @laughter.errors.full_messages.each do |msg| %>
          <p><%= msg %></p>
        <% end %>
      </div>
    <% end %>
    
<%= form_for(@laughter, html: {class: 'ui form'}) do |f| %>
<div class="ui raised segment">
    
    <div class="field">
      <label>タイトル</label>
      <%= f.text_field :title, class: "" %>
    </div>
    
    <div class="field">
      <label>内容</label>
      <%= f.text_area :content, class: "" %>
    </div>
  
  <div class="actions">
    <%= f.submit "登録", class: 'ui button'%>
  </div>
</div>
<% end %>